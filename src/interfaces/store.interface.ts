import { Document } from "mongoose";

/*

    --*-- Stored Product --*--
    product_id -> complect id
    status -> status 
    story_of_trip  -> product tarixi 


*/

export interface IStore extends Document {
    
}